# Filebeat Configuration Example
# ------------------------------------------------------------------------
# filebeat.inputs:
# - type: log
#   enabled: true
#   paths:
#     - /var/log/claude-proxy/*.log
#   json.keys_under_root: true
#   json.add_error_key: true
#   json.message_key: message

# processors:
#   - add_host_metadata: ~
#   - add_cloud_metadata: ~
#   - add_docker_metadata: ~

# output.elasticsearch:
#   hosts: ["https://REPLACE_ME_ES_HOST:9200"]
#   username: "elastic"
#   password: "REPLACE_ME_PASSWORD"
#   index: "claude-proxy-%{+yyyy.MM.dd}"

# Fluentd Configuration Example
# ------------------------------------------------------------------------
# <source>
#   @type tail
#   path /var/log/claude-proxy/*.log
#   pos_file /var/log/td-agent/claude-proxy.log.pos
#   tag claude.proxy
#   <parse>
#     @type json
#   </parse>
# </source>

# <match claude.proxy>
#   @type elasticsearch
#   host REPLACE_ME_ES_HOST
#   port 9200
#   logstash_format true
#   logstash_prefix claude-proxy
#   include_tag_key true
#   tag_key @log_name
#   flush_interval 5s
# </match>
